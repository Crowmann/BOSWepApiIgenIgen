@model IEnumerable<BosWebApiFinal.Models.Booking>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kursu.Navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ledere.Navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lokaler.Addresse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kursu.Navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ledere.Navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lokaler.Addresse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
